<%= form_for(@beadfabric) do |f| %>
  <% if @beadfabric.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@beadfabric.errors.count, "error") %> prohibited this beadfabric from being saved:</h2>

      <ul>
      <% @beadfabric.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :bead_id %><br />
    <%= f.text_field :bead_id %>
  </div>
  <div class="field">
    <%= f.label :post_id %><br />
    <%= f.text_field :post_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
