<% content_for :head do %>
  <%= javascript_include_tag "bead_ops", "message_ops" %>
<% end %>

<% content_for :leftside do %>
  <%= render :partial => current_user %>
  

<%= link_to image_tag('add.png', :border => false) + 'new message', '#', :class => 'hider', :id => 'create_message', :title => 'Create a new message' %>

<% if current_user.live_memories.present? %>
    | <%= link_to content_tag(:span, (pluralize(current_user.live_memories.count,'live message')), :class => 'new_message_count'), '#', :class => 'hider', :id => 'live_memories' %>
<% end %>

<div class="content_create_message" style="display:none">
    <%= render 'shared/post_form' %>
  </div>

  <div class="content_live_memories" style="display:none">
    <div class='defloater'></div><div class='extender_stripe'><div class='extender_header'>showing live messages</div><div class='extender_line'></div></div><br />

    <%= render :partial => current_user.live_memories %>
  </div>
  <div class="activated_post_container"></div>



  <% if incoming_trusts.present? %>
    <h2>incoming trusts</h2>
    <%= render :partial => 'trusts/incoming_trust', :collection => incoming_trusts %>
  <% end %>
  
      <div class="slot_full"></div>
<% end %>
<%# debug(current_user.posts_with_new_memories) %>

<h2>browse interests<span class="title_operator"><%# link_to image_tag('add.png', :border => false) + ' add new', new_interest_path %></span></h2>
<%# debug(@parent_beads_array) %>
  <% @parent_beads_array.each do |bead| %>
    <div class="bead_point_container" data-id="<%= bead.id %>"><%= render :partial => 'shared/bead', :object => bead %></div>
  <% end %>
  <%# render :partial => @interests %>
<% content_for :rightside do %>
  <div class="dynamic" id="recalls">
  </div>
    <h2>beads</h2>
  <h2><%= link_to 'recent activity', load_with_ajax_path(:load_type => 'active_beads'), :remote => true, :class => 'a_loader' %></h2>
  <div id="content_active_beads" style="display: none"><img src='/images/loader.gif'/> Loading...</div>
  <h2><%= link_to 'top beads overall', load_with_ajax_path(:load_type => 'top_beads_overall'), :remote => true, :class => 'a_loader' %></h2>
  <div id="content_top_beads_overall" style="display: none"></div>
  <h2><%= link_to 'recently added', load_with_ajax_path(:load_type => 'recent_beads'), :remote => true, :class => 'a_loader' %></h2>
  <div id="content_recent_beads" style="display: none"><%= render :partial => recent_beads %></div>

<% end %>

<% content_for :middletop do %>
  <% unless current_user.role == 'guest'  %>
    <p><i>full memory search</i></p>
  <p>
    <%= text_field_tag :mindsearch, params[:search], :placeholder => "search..." %>
  </p>
  <% else %>
  <p><i>complex search disabled for guests</i></p>
  <% end %>

<% end %>
