<% content_for :leftside do %>
  <%= render :partial => current_user %>
	<% if incoming_trusts.present? %>
		<h2>incoming trusts</h2>
		<%= render :partial => 'trusts/incoming_trust', :collection => incoming_trusts %>
	<% end %>
                <div class="slot_full"></div>
<% end %>

  <h2>my interests</h2>
  <%= render :partial => @interests %>
<% content_for :rightside do %>
<%# link_to "#{live_messages(current_user).size} live messages", '#', :remote => true, :class => 'slider', :id => 'live_messages' %>
  <%# render :partial => 'posts/activated_post', :collection => live_messages(current_user.id) %>

  <h2>beads</h2>
  <h2><%= link_to 'recent activity', beads_path, :remote => true, :class => 'slider', :id => 'active_beads' %></h2>
  <div class="content_active_beads" style="display: none"><%= render :partial => active_beads %></div>
  <h2><%= link_to 'top beads overall', '#', :remote => true, :class => 'slider', :id => 'top_beads' %></h2>
  <div class="content_top_beads" style="display: none"><%= render :partial => top_beads_overall %></div>
  <h2><%= link_to 'recently added', '#', :remote => true, :class => 'slider', :id => 'recent_beads' %></h2>
  <div class="content_recent_beads" style="display: none"><%= render :partial => recent_beads %></div>
<% end %>

<% content_for :bottom do %>
  <table width="100%" class="bottom_stripe">
  <tr>
    <td class="leftside"></td><td class="middle"><%= link_to image_tag('add.png', :border => false) + ' add new interest', new_interest_path %> </td><td class="rightside"></td>
  </tr>
</table>
<% end %>
