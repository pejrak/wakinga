<h2><%= @interest.title %> (<%= (@interest.post_count_unread > 0)? h(@interest.post_count_unread.to_s + ' new / ') : '' %><%= @interest.post_count %>)</h2>
  <% @interest.beads.each do |beader| %>
  o <span class="text_emphasizer"><%= beader.title %></span>
  <% end %>
  <p>
  last visit <%= time_ago_in_words(@interest.last_visit_at) %> ago<br />
  with <%= current_user.good_memories.size %> memories<br />
  you share it with <%= pluralize(@shared_by_this_many_users, 'mind') %>
  <% if @shared_by_this_many_users > 0 %>
    (<% @sharing_user_ids.each do |uid| %>
      <%= User.find_by_id(uid).username %><%= (@shared_by_this_many_users > 1)? ', ':''  %>
    <% end %>)
  <% end %>
  </p>
<% if current_user == @interest.user and @interest.trusts.present? %>
	<br /><h3>You trust this interest to</h3>
	<%= render :partial => 'trusts/outgoing_trust', :collection => @interest.trusts %>
<% end %>