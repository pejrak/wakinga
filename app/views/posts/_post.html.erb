<%= div_for post do %>
	<h2><%= link_to_unless_current h(post.title), post %></h2> by <strong><%= post.user.username %></strong> ( <%= post.comments.count %> )
	<%= simple_format h(post.content) %>
<% end %>