	<h3>from <%= link_to(incoming_trust.interest.user.username, user_path(incoming_trust.trustor)) %></h3>

<% incoming_trust.interest.beads.each do |beader| %>
  o <span class="text_emphasizer"><%= beader.title %></span>
<% end %>

<%= (incoming_trust.shared?)? 'confirmed' : 'not confirmed' %>
 (<%= link_to 'x', incoming_trust, :confirm => 'Are you sure?', :method => :delete %>)
